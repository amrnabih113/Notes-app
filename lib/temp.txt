Center(
          child: Column(
            children: [
              MaterialButton(
                onPressed: () async {
                  try {
                    int responcce = await MyDb.insert(
                        "INSERT INTO 'USERS' ('NAME','EMAIL','PASSWORD') VALUES ('AMR NABIH','amrnabih113@gmail.com','amrnabih112')");
                    print(responcce.toString());
                  } catch (e) {
                    print(e.toString());
                  }
                },
                shape: ContinuousRectangleBorder(
                    borderRadius: BorderRadius.circular(20)),
                color: Colors.purple,
                minWidth: 60,
                height: 40,
                child: const Text("insert data"),
              ),
              const SizedBox(
                height: 40,
              ),
              MaterialButton(
                onPressed: () async {
                  List data = await MyDb.getdata("SELECT * FROM 'USERS'");
                  print("$data");
                },
                shape: ContinuousRectangleBorder(
                    borderRadius: BorderRadius.circular(20)),
                color: Colors.purple,
                minWidth: 60,
                height: 40,
                child: const Text("get data"),
              ),
              const SizedBox(
                height: 40,
              ),
              MaterialButton(
                onPressed: () async {
                  int responce = await MyDb.update(
                      "UPDATE USERS SET EMAIL = 'amrnabih@gmail.com' WHERE ID = 1 ");
                  print(responce);
                },
                shape: ContinuousRectangleBorder(
                    borderRadius: BorderRadius.circular(20)),
                color: Colors.purple,
                minWidth: 60,
                height: 40,
                child: const Text("update data"),
              ),
              const SizedBox(
                height: 40,
              ),
              MaterialButton(
                onPressed: () async {
                  int responce =
                      await MyDb.delete("DELETE FROM USERS WHERE ID = 2");
                  print(responce);
                },
                shape: ContinuousRectangleBorder(
                    borderRadius: BorderRadius.circular(20)),
                color: Colors.purple,
                minWidth: 60,
                height: 40,
                child: const Text("delete data"),
              ),
              const SizedBox(
                height: 40,
              ),
            ],
          ),
        ),